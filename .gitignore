.build/
build/

# Generated by scripts/render-app-icon.swift + iconutil. The icon lives in
# the repo as the script that draws it, not as a binary blob.
AppIcon.icns
.swiftpm/
*.xcodeproj
xcuserdata/
DerivedData/
.DS_Store

# Local-only development docs — issue drafts, specs, plans.
# Kept in the working tree for the author's reference but never
# committed / pushed. Actual issues live on GitHub; this is a staging area.
.issues/
docs/superpowers/
.superpowers/
